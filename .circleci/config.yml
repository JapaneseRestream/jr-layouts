version: 2

jobs:
  build:
    machine: true
    steps:
      - run:
          name: Deploy
          command: |
            ssh -o "StrictHostKeyChecking no" $SSH_USER@$SSH_HOST "echo hogehoge"
            ssh -o "StrictHostKeyChecking no" $SSH_USER@$SSH_HOST "nvm current"
            ssh -o "StrictHostKeyChecking no" $SSH_USER@$SSH_HOST "which node"
            ssh -o "StrictHostKeyChecking no" $SSH_USER@$SSH_HOST "node -v"
