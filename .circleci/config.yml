version: 2

jobs:
  build:
    machine: true
    steps:
      - run:
          name: Deploy
          command: |
            ssh -o "StrictHostKeyChecking no" $SSH_USER@$SSH_HOST " \
              echo hogehoge && \
              export NVM_DIR=~/.nvm && \
              source ~/.nvm/nvm.sh && \
              nvm current
            "
