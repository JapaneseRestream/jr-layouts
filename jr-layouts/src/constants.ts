export const enum Replicant {
	Schedule = 'schedule',
	CurrentRun = 'current-run',
	Twitch = 'twitch',
}

export const enum Message {
	PreviousRun = 'previous-run',
	NextRun = 'next-run',
	UpdateSchedule = 'update-schedule',
}
